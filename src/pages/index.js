import React from "react"
import { Link } from "gatsby"

import Layout from "../components/layout"
import Image from "../components/image"
import SEO from "../components/seo"
import NavHeader from "../components/navbar"

/*const IndexPage = () => (
  <Layout>
    <SEO title="Home" />
    <h1>Hi people</h1>
    <p>Welcome to your new Gatsby site.</p>
    <p>Now go build something great.</p>
    <div style={{ maxWidth: `300px`, marginBottom: `1.45rem` }}>
      <Image />
    </div>
    <Link to="/page-2/">Go to page 2</Link>
  </Layout>
)*/ 

const IndexPage = () => (
  <NavHeader />


  // @@TODO items :

  // content sections - services / contact me / gallery
  // services - static markup - combination of <ul> tags
  // contact me - google map - salon info - contact info - message me functionality
  // gallery 

  // a content compoenont 
  // a contact me specific section


  // navbar stlying UI
  // will use bootstrap 
  // remove drop down 
  // add instagram icon - need to script in fa icons (font awesome) for each page
  // other tabs wille - home - services - contact - gallery - plus instagram / other social if wanted 
  // center the tabs 
  // add hover state stlying to tabs 
  

  // we need a logo - will need to be a .png
  // decide on the landing view 
  // we need image assets 
  // we need design input from macey
  // we need color scheme input from macey 

  // add google analytics tags 
  // need to set up an account with google for this

  



)

export default IndexPage
